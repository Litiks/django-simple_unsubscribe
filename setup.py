# -*- coding: utf-8 -*-
from setuptools import setup, find_packages

setup(
  name='django-simple_unsubscribe',
  description='A very simple (email) unsubscribe management addon for Django',
  packages=['unsubscribe'],
)
